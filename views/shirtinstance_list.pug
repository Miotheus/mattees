extends layout

block content
  h1= title

  ul
  each val in shirtinstance_list
    li 
      a(href=shirtinstance.url)  #{shirtinstance.shirt}
      #{shirtinstance.status}

  else
    li There are no shirt copies in this store.